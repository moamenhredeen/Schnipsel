services:
  postgres:
    image: 'postgres:latest'
    environment:
      - 'POSTGRES_DB=kakeradb'
      - 'POSTGRES_PASSWORD=kakerapassword'
      - 'POSTGRES_USER=kakerauser'
    ports:
      - '5432:5432'
  keycloak:
    image: 'quay.io/keycloak/keycloak:latest'
    command: start-dev
    environment:
      - 'KEYCLOAK_ADMIN=admin'
      - 'KEYCLOAK_ADMIN_PASSWORD=admin'
      - 'KC_HTTP_PORT=8090'
    ports:
      - '8090:8090'
